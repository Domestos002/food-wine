<!-- build:js js/vendor.js -->
<script src="bower_components/jquery/dist/jquery.min.js"></script>
<script src="bower_components/owl.carousel/dist/owl.carousel.min.js"></script>
<script src="http://masonry.desandro.com/v2/jquery.masonry.min.js"></script>
{#
<script src="bower_components/masonry/masonry.js"></script>
#}
{#<script src="js/input.mask.js"></script>#}
{#<script src="js/PP.js"></script>#}
<!-- endbuild -->

<!-- build:js js/common.js -->
<script src="js/custom.js"></script>
<!-- endbuild -->
<script>
    $(document).ready(function () {
        $(".testimonials-quoters").owlCarousel({
            items: 3,
            loop: true,
            nav: true,
            dots: false,
//            mouseDrag: true,
            autoHeight: true
            /*responsive: {
             0: {
             items: 1
             }
             }*/
        });
    });
    $(document).ready(function () {
        $(".first-screen-slider").owlCarousel({
            items: 1,
            loop: true,
            dots: false,
            nav: true,
            mouseDrag: true,
            responsive: {
             0: {
             items: 1
             }
             }
        });
    });
    $(function () {
        $('#masonry-container').masonry({
            // options
            itemSelector: '.masonry-item',
            singleMode: false,
            isResizable: true,
            isAnimated: true,
            isFitWidth: true,
            animationOptions: {
                queue: false,
                duration: 500
            }
        });
    });
    $(function () {
        $('#masonry-container').masonry({
            // options
            itemSelector: '.masonry-item',
            singleMode: false,
            isResizable: true,
            isAnimated: true,
            isFitWidth: true,
            animationOptions: {
                queue: false,
                duration: 500
            }
        });
    });
    /*$('#masonry-container').masonry({
        itemSelector: '.masonry-item',
        columnWidth: function( containerWidth ) {
            return containerWidth / 3;
        }
    });*/
</script>
